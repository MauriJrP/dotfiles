# ~/.config/ghostty/config

font-family = "JetBrainsMono Nerd Font"
font-size = 14

# Window look & feel
window-decoration = auto
window-padding-x = 8
window-padding-y = 8
copy-on-select = true
theme = Dracula
#window-theme = dark

# Close behavior
confirm-close-surface = false
quit-after-last-window-closed = true

# Optional: start tmux automatically (comment out if you prefer plain shell)
# command = tmux new -A -s main

